# Test dependencies for packages/api
# Install with: pip install -r requirements-test.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # required by FastAPI TestClient for async support
pytest-cov>=5.0.0
